# Variables are the containers to store data of any data type.
# Variables can be re-assigned.
# Variable names are case sensitive.
# Reserved Keywords cannot be uses as a variable name.
# Variable name can be alphaNumeric and contains underscores.
# Variable name can only start with an alphabet.

# ------------------------------------------------------------------------------------------------------------------------------------------
name = "Jack"
print(name)

name = "Angela"
print(name)

name = input("What's your name?")
length = len(name)
print(name)

Name = 'Maya'
print(Name)

this_lang = "Python"
print(this_lang)
# ------------------------------------------------------------------------------------------------------------------------------------------

# Name Error:
# Occurs when the interpreter encounters a variable, function, or module that it cannot find or recognize.
